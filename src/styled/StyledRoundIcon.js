import styled from "styled-components";

const StyledRoundIcon = styled.div`
    height: 40px;
    width: 40px;
    background-color: ${props=>props.bg};
    border-radius: 50%;

`;

export default StyledRoundIcon;